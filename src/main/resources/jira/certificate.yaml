apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: jira-cert
  namespace: default
spec:
  secretName: jira-secret
  commonName: fqdn
  dnsNames:
  - fqdn
  issuerRef:
    name: letsencrypt-staging-issuer
    kind: ClusterIssuer