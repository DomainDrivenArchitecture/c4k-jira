kind: PersistentVolume
apiVersion: v1
metadata:
  name: jira-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 30Gi
  hostPath:
    path: "/var/jira"
